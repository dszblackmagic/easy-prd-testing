# 测试用例

## 说明

- 优先级口径：`P0` 表示主流程或阻断性高风险场景；`P1` 表示高频重要场景；`P2` 表示非主路径但需覆盖的补充场景；`P3` 表示低频或观察性场景。
- 追踪规则：`场景` 字段或用例编号前缀建议带 `F-xxx` 功能编号，用于回连 `01-模块拆解.md` 中的功能拆解记录。
- 执行归属：每条用例必须按优先级归入 `执行结果/P0/`、`执行结果/P1/`、`执行结果/P2/` 或 `执行结果/P3/`，实际执行记录写入对应目录下的 `05-执行记录.md`。

| 用例编号 | 优先级 | 执行目录 | 模块 | 场景 | 前置条件 | 测试步骤 | 预期结果 | 是否字段比对 | 是否可能触发视频 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| MOD-P0-001 | P0 | `执行结果/P0/` |  |  |  |  |  | 是 | 否 |
