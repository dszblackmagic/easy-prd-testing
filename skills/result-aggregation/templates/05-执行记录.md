# 执行记录

说明：本模板可用于优先级目录下的独立执行记录，也可用于模块根目录的最终汇总执行记录。优先级目录中的记录只写本级别用例；根目录中的记录只写汇总结果和证据索引。

## 状态说明

- `通过`：执行结果与 `PRD` 预期一致。
- `失败`：已完成执行且确认存在偏差，可继续判断是否进入缺陷。
- `阻塞`：因账号、权限、测试数据、环境或依赖条件问题无法继续执行，先记录阻塞原因，默认不直接进入缺陷。
- `未测`：本轮未执行或被排期跳过。

证据规则：通过用例不需要截图证明，截图证据和视频证据可留空；字段差异、失败、阻塞或高风险提示场景按规则补充证据。

## 1. 执行批次信息

| 字段 | 内容 |
| --- | --- |
| 执行日期 |  |
| 测试环境 |  |
| 执行人 |  |
| 执行 agent |  |
| 所属优先级 | P0 / P1 / P2 / P3 / 汇总 |
| 来源目录 |  |
| 浏览器 | Chrome / Playwright |
| `PRD` 文件 |  |
| 测试路径 |  |
| 登录方式 | 指定账号 / 复用当前登录态 / 无需登录 |
| 执行模式 | 单 agent / 多 agent |
| 高风险动作授权 | 允许真实提交 / 仅验证入口、展示、校验、二次确认和提示 |

## 2. 执行前确认汇总

| 确认项 | 结论 | 说明 |
| --- | --- | --- |
| `01-04` 必备资料已定位且可读取 | 是 / 否 | 默认资料已由用户确认，仅记录存在性检查结果 |
| 测试环境和测试路径已确认 | 是 / 否 |  |
| 登录方式和权限范围已确认 | 是 / 否 |  |
| 执行模式已确认 | 是 / 否 |  |
| 高风险动作授权边界已确认 | 是 / 否 |  |

## 3. 执行明细

| 用例编号 | 优先级 | 状态 | 实际结果摘要 | 截图证据 | 视频证据 | Console 摘要 | Network 摘要 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |  |  |

## 4. 字段一致性校验

| 区域 | 预期字段 | 实际字段 | 差异分类 | 证据 |
| --- | --- | --- | --- | --- |
| 查询区 |  |  |  |  |

说明：当差异分类为“语义疑似不一致”时，先按 `PRD` 文案记录预期，再补充原型图、页面文案或交互含义的冲突说明，并在证据或备注中标明后续动作（如“待产品确认”“需补充口径”“暂不判缺陷”）。

## 5. 过程输出摘要

| 时间 | 阶段 | 对用户同步的关键信息 | 关联证据或记录 |
| --- | --- | --- | --- |
|  |  |  |  |

## 6. 汇总索引

仅根目录汇总记录需要填写本节；优先级目录记录可留空。

| 优先级 | 执行目录 | 用例总数 | 通过 | 失败 | 阻塞 | 未测 | 证据目录 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| P0 | `执行结果/P0/` |  |  |  |  |  |  |  |
| P1 | `执行结果/P1/` |  |  |  |  |  |  |  |
| P2 | `执行结果/P2/` |  |  |  |  |  |  |  |
| P3 | `执行结果/P3/` |  |  |  |  |  |  |  |
